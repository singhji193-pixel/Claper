{
  "name": "claper-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E suite for the NextGen Summit attendee PWA",
  "scripts": {
    "seed": "docker exec clapper-app-1 mix run e2e/seed.exs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
